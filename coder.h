#ifndef CODER_H
#define CODER_H

int encode(uint32_t code_point, CodeUnits *code_units)
  if(){
    return 0;
  }else{
    return -1;}
uint32_t decode(const CodeUnit *code_unit)
  if(){
    return 0;
  }else{
    return -1;
